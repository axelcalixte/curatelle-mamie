{
  "name": "test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "deno run -A --node-modules-dir node_modules/.bin/vite",
    "build": "deno run -A --node-modules-dir node_modules/.bin/vite build",
    "preview": "deno run -A --node-modules-dir node_modules/.bin/vite preview"
  },
  "dependencies": {
    "bulma": "^1.0.4"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  }
}
